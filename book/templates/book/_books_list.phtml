{% load i18n %}
{% load static_tags %}
{% load_css static /book/style.css %}
{% if showcheckbox and not onetime_show_notification.SELECT_BOOK_IN_LIST %}
<div class="row">
	<div class="col-lg-12">
		<div class="bs-example">
			<div class="alert alert-dismissable alert-info">
				<button type="button" class="close onetime-notification" data-dismiss="alert" data-key="SELECT_BOOK_IN_LIST">×</button>
				<p>
					{% trans "You can select book in the list by clicking to the spacing in book title block" %}.
					<img src="{{ STATIC_URL }}img/book/help/book_select_before_click.png" />
					<img src="{{ STATIC_URL }}img/book/help/book_select_after_click.png" />
				</p>
			</div>
		</div>
	</div>
</div>
{% endif %}
<div id="books" class="list {{class}}">
    <div class="row">
        <div class="col-lg-12">
            <table class="table table-striped table-bordered table-hover">
                <thead>
                    <tr>
                        <th width="50%">{% trans 'Book name' %}</th>
                        <th width="30%">{% trans 'Categories' %}</th>
                        <th>{% trans 'Author' %}</th>
                    </tr>
                </thead>
                <tbody>
                    {% for book in books %}
                    <tr class="item book {% if not book.is_read and not book.is_read_by_current_user %}unread{% endif %}"
                    item_id="{{book.id}}" {% if not book.is_read and not book.is_read_by_current_user %}unread="yes"{% endif %}>
                        <td>
                            {% if showcheckbox %}
                                <input type="checkbox" name="ids[]" value="{{book.id}}">
                            {% endif %}
                             <a href="{{ book.full_url }}" class="title">{{book.title}}</a></td>
                        <td>
                            {% for category in book.categories.all %}
                            <a class="label label-info" href="#">{{category.title}}</a>
                            {% endfor %}
                        </td>
                        <td><a href="books/author/{{book.author.slug}}">{{book.author.name}}</a></td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>